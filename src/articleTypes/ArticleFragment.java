package articleTypes;

/**
 * Article fragment (e. g. no sources, only one paragraph)
 */
public interface ArticleFragment {

    /**
     * Prints the fragment.
     */
    void printArticleFragment();

}